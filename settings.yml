# Active configuration for this checkout. See settings.example.yml for docs.
# Edit the paths below for your machine.

# Heavy run/event ROOT files (kept outside the repo). Single path or a list of
# search roots (first match wins). Absolute paths in data_reference*.yml win.
data_dir:
  - /eos/home-o/oarquero/TB2026CERN

# Calibration files (vendored copies live in ./calibration).
calib_dir: ./calibration

# Geometry inputs (pad maps, slab z, tungsten map).
geometry_dir: ./geometry

# Run-list configs.
configs_dir: ./configs/data

# Validation output base.
output_dir: ./validation_output

# Where the *.valcache.root caches go. null = next to each input file.
cache_dir: null
